.form-wrap
  %h2.form-title
    EventGeek
  .test-account__description
    こちらのアカウントでお試しください
    %br/
    アドレス : test100@com.ne.jp
    %br/
    パスワード : abc123
    %br/
    %br/
  = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
    .form-field
      = f.label :email
      %br/
      = f.email_field :email, autofocus: true, autocomplete: "email", class: "input-field", placeholder: 'PC・携帯どちらでも可', required: true
    .form-field
      = f.label :password
      %br/
      = f.password_field :password, autocomplete: "current-password", class: "input-field", placeholder: '6文字以上', required: true
    - if devise_mapping.rememberable?
      .form-field
        = f.check_box :remember_me
        = f.label :remember_me
    .actions
      = f.submit "Log in", class:"form-submit"
  -# = render "devise/shared/links"
  .form-footer
    = link_to "/registration" do
      アカウントを持っていない場合は登録してください
  .copyright
    Copyright©RYO KURASHIMA
